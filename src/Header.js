import React from 'react';

const Header = () => {
  const headerStyle = {
    backgroundColor: 'royalblue',
    color: '#fff'
  };

  return (
    <header style={headerStyle}>
      <h1>Love With Passion</h1>
    </header>
  );
};

export default Header;
